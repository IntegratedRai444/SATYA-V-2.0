# SATYA-V-2.0 Lightweight Backend Requirements
# Essential dependencies only for faster startup

# Web framework (essential)
fastapi
python-multipart

# Core ML (essential)
numpy>=1.21.0
scikit-learn>=1.0.0

# PyTorch (CPU only for speed)
torch>=2.0.0
torchvision>=0.15.0

# Basic image processing
Pillow>=10.0.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0
fpdf>=1.7.0

# Testing
pytest
httpx

# Optional (install separately if needed)
# opencv-python-headless>=4.8.0
# librosa>=0.10.0
# mediapipe>=0.10.0
# face-recognition>=1.3.0
# dlib>=19.24.0 