# SATYA-V-2.0 Backend Requirements
# Unified and deduplicated for production use
# Python 3.13+ compatible

# Web framework
fastapi
python-multipart

# Core ML and AI
numpy>=1.21.0
scikit-learn>=1.0.0
scipy>=1.7.0
pandas>=1.5.0

# PyTorch (main deepfake models)
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0

# Computer Vision
opencv-python-headless>=4.8.0
Pillow>=10.0.0
face-recognition>=1.3.0
# dlib requires CMake and a C++ compiler to build from source on Python 3.13+
dlib>=19.24.0
# Mediapipe (optional, may not work on Python 3.13+)
mediapipe>=0.10.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.0
pydub>=0.25.0

# Video Processing
moviepy
ffmpeg-python

# Data Visualization
matplotlib>=3.6.0
seaborn>=0.12.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0
tqdm>=4.65.0
joblib>=1.3.0
piexif
exifread
fpdf>=1.7.0

# Dev/test dependencies
pytest
httpx
fastapi[all]

# Optional: GPU acceleration (uncomment if needed)
# cupy-cuda11x>=12.0.0  # Uncomment for CUDA support

# Flask (legacy/optional, not used in main backend)
# flask>=2.3.0
# flask-cors>=4.0.0
# Werkzeug>=2.3.0

# TensorFlow and mediapipe are NOT supported on Python 3.13 as of June 2024 